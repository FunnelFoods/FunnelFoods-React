import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    field: {
        textAlign: "left",
        color: "white",
        fontSize: 30,
    },
    input: {
        height: 40,
        borderBottomColor: "white",
        borderBottomWidth: 2,
        color: "white"
    },
});

export { styles };
